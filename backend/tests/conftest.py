"""Pytest configuration and fixtures.

Async tests run under ``pytest-asyncio`` in ``auto`` mode (see
``[tool.pytest.ini_options]`` in ``pyproject.toml``); no event-loop fixture
override is needed here.
"""
